export interface AbilityInterface{
    ability: {
        name: string
        url: string
    }
    is_hidden: boolean
    slot: number
}

export interface MoveInterface{
    move: {
        name: string
        url: string
    }
}